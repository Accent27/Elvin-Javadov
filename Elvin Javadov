    number = int(input("Введите число: "))
    if number > 7:
        print("Hello")
except ValueError:
    print("Пожалуйста, введите корректное целое число.")
name = input("Введите имя: ")
if name == "John":
    print("Hello, John")
else:
    print("Такого имени нет")

# массив
try:
    array = list(map(int, input("Введите массив чисел через пробел: ").split()))
    print("Элементы массива, кратные 3:")
    found = False
    for num in array:
        if num % 3 == 0:
            print(num)
            found = True
    if not found:
        print("Нет элементов, кратных 3.")
except ValueError:
    print("Пожалуйста, введите корректные целые числа для массива.")
